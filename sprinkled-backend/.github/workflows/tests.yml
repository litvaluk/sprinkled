name: Tests

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Unit Tests
        run: |
          npm install 
          npm run test
  e2e:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run E2E Tests
        run: |
          npm install 
          npm run test:e2e